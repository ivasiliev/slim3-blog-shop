<div class="add_comment">
        {% if userdata.id and userdata.id %}
                <input type="hidden" comment_data="userid" value="{{ userdata.id }}"> {# current user id #}
                <input type="hidden" comment_data="parent" value=""> {# current comment id - answer to other comment #}
                <input type="hidden" comment_data="curr_id" value=""> {# current post id #}
                <textarea placeholder="Комментарий:" comment_data="txt"></textarea>
                <button type="button">Оставить комментарий</button>
        {% else %}
                <div class="comment_need_auth">
                        <span>Войдите, чтобы оставить комментарий</span>
                </div>
        {% endif %}        
</div>